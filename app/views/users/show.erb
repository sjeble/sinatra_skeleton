<%= erb(:"_navigation") %>

<h2><%= "#{@user.first_name} #{@user.last_name}" %></h1>

<h3>User channels:</h2>
  <ul>
    <% @user.channels.each do |channel| %>
      <li><a href="/channels/<%= channel.id %>"><%= channel.name %></a></li>
    <% end %>
  </ul>
  <% if @user.channels.length >= 1 %>
    <p>Monthly bill: $<%= @user.monthly_bill %></p>
  <% end %>
</div>
